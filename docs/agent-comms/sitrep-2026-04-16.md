# Sitrep — 2026-04-16

## Status at handoff
- **Branch:** main (clean worktree, 44 commits ahead of origin/main)
- **Last merge:** `b597927` (2026-04-12) — cr/silo-20260412-184222-claude (config preset for inventory filtering)
- **Versions:** silo-dashboard.py 2.5.4, silo_client_rt.py 1.4.2

## Recent accomplishments
- ✅ RT `TRK` column now resolves tracker keys from traktor registry (not just hostnames)
- ✅ RT completed items with messages no longer collapse to `error`
- ✅ Config preset option for filtering inventory
- ✅ Tracker verification: td.jumbohostpro.eu → torrentday, speed.connecting.center → speedcd, etc.

## Open TODOs (ranked by impact)

### P2 — User-Visible UX
**P2-B/C: Scope filtering (w/u/v/e/g) broken for rTorrent and SABnzbd**
- Root cause: filter checks `raw.state` (qBit field); RT and SABnzbd use different state storage
- Fix: use unified `state` key in `rows_to_render` filter block
- Impact: pressing scope keys on non-qBit clients shows nothing
- File: `bin/silo-dashboard.py`, lines ~900–950 (approximate)

**P2-A: MediaInfo cache misses block render**
- Root cause: synchronous `mediainfo` call in render path
- Fix: return "…" placeholder on miss; background thread enqueues hash for fetch
- File: `bin/silo-dashboard.py`, `get_mediainfo_summary_cached()`

### P3 — Code Quality
**P3-B: qbit-* shim removal (scheduled 2026-07-01)**
- Deprecation warnings already in place
- Files: `bin/qbit-*.py` shims

**P3-C: SABnzbd last_error silent loss**
- Low-priority; queue error overwritten if history fetch succeeds after queue fails
- File: `bin/silo_client_sab.py`

### P4 — Future Capability
- SABnzbd extra actions (category change, mark-as-completed)
- rTorrent SCGI socket transport (remove nginx dependency)
- SABnzbd cache daemon

## Validation shortcuts
```bash
python3 -m py_compile bin/silo_client_rt.py bin/silo-dashboard.py
PYTHONPATH=bin python3 -c "from silo_client_rt import *; print('Syntax OK')"
```

## Architecture notes
- Unified client modules: `silo_client_rt.py`, `silo_client_sab.py` (same protocol)
- Dashboard cycles clients via `\` key: qBit → rTorrent (if configured) → SABnzbd (if configured)
- rTorrent tracker data: fetched via `system.multicall` in 500-hash batches, 300s TTL
- Tracker resolution: local traktor registry (`/home/michael/dev/tools/traktor/config/tracker-registry.yml`)
